# Internal port the Go server listens on (behind Nginx reverse proxy)
SERVER_PORT=8080

# Directory for storing downloaded video content
CONTENT_DIR=./content

# Domain name for Nginx and Let's Encrypt certificate
DOMAIN=example.com

# Email for Let's Encrypt certificate notifications (expiry warnings, etc.)
CERTBOT_EMAIL=admin@example.com
